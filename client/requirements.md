## Packages
@react-three/fiber | React renderer for Three.js (3D car model)
@react-three/drei | Helpers for Three.js (OrbitControls, Stage, etc.)
three | Three.js core library
framer-motion | Complex page and scroll animations

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
